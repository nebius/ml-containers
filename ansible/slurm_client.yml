---

- name: Install slurm client
  hosts:
    - localhost
    - all
  become: true
  gather_facts: true
  gather_subset:
    - min
  roles:
    - slurm_client
    - slurm_divert
  tags:
    - slurm_client
